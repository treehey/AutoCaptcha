# 插件商店描述

## 短描述

南大统一身份认证滑块与选课点击验证码本地自动登录助手，无需大模型 API。

## 商店地址

https://microsoftedge.microsoft.com/addons/detail/hebfkinlcalfnmeeeaciaghoialpmnfp

## 详细描述

NJU Login Pro 是一款为南京大学师生设计的浏览器扩展，覆盖两条日常登录流程：南大统一身份认证当前使用的拼图滑块安全验证，以及选课系统的中文点击验证码登录。扩展会在本地完成安全验证、填写已保存的学号或工号与密码并提交登录表单；选课系统登录后还可继续监控课程余量并自动尝试选课。

扩展的验证码处理完全在浏览器本地完成，不依赖 GPT、Claude 或任何第三方验证码识别接口，也无需配置 API Key。统一身份认证使用浏览器原生图像处理定位拼图缺口，完成认证页原有的安全验证；若页面回退为旧版图形验证码，扩展仍会使用轻量彩色字符 CNN 和 Tesseract.js OCR。选课系统使用随扩展打包的 ONNX/WASM 匹配模型。验证码图像和登录凭证不会发送给第三方服务，账号配置只保存在浏览器本地存储中。

选课登录页会在本地匹配底部目标字与上方候选字；置信度不足、三字验证码或背景不匹配时会换图重试，达到门槛后才按顺序点击、填充账号密码并提交。选课系统自动登录和统一身份认证自动登录可在 Popup 中独立开启或暂停。登录后，用户可在“选课助手”中填写目标课程名称，扩展会持续监控课程余量，发现可选名额后自动点击“选择”并确认弹窗。

NJU Login Pro 是开源项目，用户可以通过插件面板中的 GitHub 入口查看源码、提交问题或参与改进。项目目标是保持轻量、透明、可审计，让经常访问南大统一身份认证和选课系统的用户获得更快、更少打扰的使用体验。

## 主要功能

- 统一身份认证滑块验证：本地定位拼图缺口、构造验证轨迹并完成认证页安全验证；旧版图形验证码保留 CNN/OCR 兼容路径。
- 无需大模型 API：不依赖云端模型、第三方打码平台或外部识别服务。
- 凭证自动填充：在浏览器本地保存并填入学号或工号、密码和识别出的验证码。
- 自动登录控制：支持开启或关闭自动登录，支持识别后自动提交。
- 强制填充模式：在浏览器自动填充或页面脚本干扰时，主动刷新表单值。
- 选课系统自动登录：本地识别点击验证码，低置信自动换图重试；通过门槛后按顺序点击、填写账号密码并提交登录。
- 选课助手：登录后按课程名称持续监控余量并自动尝试抢课。
- 实时监控日志：在 Popup 中查看每轮课程检测、满员、成功或异常状态。
- 隐私优先：验证码图片、账号信息和配置均留在浏览器本地。
- 开源透明：源码公开，便于用户审查、反馈和贡献。

## 工作原理

1. 安装扩展后，在浏览器工具栏打开 NJU Login Pro。
2. 在“登录助手”中填写学号或工号与密码并保存；统一认证和选课系统的自动登录开关可独立控制。
3. 访问任一支持的登录页：
   - 统一身份认证页会完成当前拼图滑块安全验证，填写账号、密码后按页面原有格式提交；旧版图形验证码页面继续使用本地 CNN/OCR。
   - 选课登录页会匹配底部目标字与上方候选字；通过门槛后依次点击候选字、填写账号密码并提交。三字验证码、低置信结果或背景不匹配时会先换图重试。
4. 两条登录流程的开关互不影响；如只需要其中一个站点，可在 Popup 中单独暂停另一个。
5. 登录选课系统后，可在“选课助手”中填写目标课程名称并开始监控；出现可选名额时，扩展会自动点击“选择”并确认弹窗。

## 隐私与权限说明

- 不收集个人数据，不上传验证码图片、账号、密码或选课信息。
- 学号或工号、密码和开关配置仅通过 Chrome Storage 保存在用户浏览器本地。
- 统一身份认证滑块定位与旧版 CNN/Tesseract 回退、选课点击验证码 ONNX Runtime Web WASM 推理都在浏览器本地执行，不加载远程模型或脚本。
- 扩展内容脚本只匹配南大统一身份认证和选课系统相关页面。
- 选课助手仅查询已打开的 `xk.nju.edu.cn` 标签页，用于向页面脚本发送开始或停止监控指令。
- 扩展不读取浏览器历史数据库，不使用外部统计、追踪或验证码识别服务。
- 隐私政策见 `PRIVACY.md`。

## 当前支持范围

- 支持：南大统一身份认证当前拼图滑块的本地安全验证、账号密码自动填充和自动登录；旧版图形验证码保留兼容。
- 支持：选课系统点击式验证码本地识别、自动点击与同表单自动登录。
- 支持：选课系统登录后的课程监控与自动抢课。
