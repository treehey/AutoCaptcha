# 隐私政策

最后更新：2026-08-24

本政策适用于 NJU Login Pro（NJU 自动登录助手）浏览器扩展。项目不包含广告、统计、行为分析或第三方验证码识别服务。

## 处理的数据

扩展会根据用户配置，在浏览器扩展本地存储中保存：

- 南京大学学号或工号及统一身份认证密码；
- 统一认证、无感登录、选课登录等功能开关；
- 课程关键词、用户加入监控的教学班 ID、课程展示信息、刷新间隔和监控状态；
- 仅在源码开发界面中产生的验证码测试样本和诊断状态。

扩展运行时会读取完成当前功能所必需的页面内容：

- 统一身份认证页面的登录表单、拼图挑战图片及验证结果；
- 旧版认证页面出现时的四位图形验证码；
- 选课登录页的点击验证码；
- 选课页面的课程名称、余量状态及选择控件。

## 数据用途和网络请求

这些数据只用于自动填写、页面安全验证、登录、课程监控以及在扩展面板中显示状态。

- 统一认证请求直接发送到 `https://authserver.nju.edu.cn/`。
- 选课登录、课程查询和选课操作只发生在 `https://xk.nju.edu.cn/`。课程监控会在页面内复用该站最近一次原生课程查询格式发起有界查询；凭证和完整请求体不会发送给开发者或第三方服务，选课提交仍调用网站原生控件。
- 滑块定位、旧版验证码 CNN/Tesseract 识别和选课点击验证码 ONNX/WASM 推理均在浏览器本地执行。
- 扩展不向开发者或第三方服务器发送账号、密码、验证码图片、课程关键词、页面内容或诊断日志。
- 扩展不加载远程代码，不调用大模型 API 或云端打码服务。

## 本地存储和凭证风险

- 账号配置和版本化课程监控配置通过 Chrome/Edge 扩展的 `storage.local` 保存在当前浏览器配置文件中。精确课程目标可包含批次、教学班类别、课程/教学班 ID，以及页面可获得的课程名、教师、时间和校区展示信息。
- 扩展不会对保存的密码再做一层独立加密。密码的保护依赖操作系统账号、浏览器配置文件和设备本身的安全性。
- 不应在公共电脑、共享浏览器配置文件或不受信任的设备上保存凭证。
- “无感登录”的本次浏览器会话状态保存在 `storage.session`，浏览器会话结束后不作为长期配置保留。
- 运行中的课程监控会把任务标识、白名单化课程目标、教学班候选 ID、进度、最近日志和选课站内返回路径保存到 `storage.session`，仅用于同一选课标签页刷新或登录失效后恢复；不保存 DOM、token、Cookie、学号、查询参数、请求体或加密提交参数。

## 无感登录

该功能默认关闭。启用后，扩展会在满足账号已配置、统一认证自动提交已开启等条件时，由扩展 Service Worker 在每个浏览器会话中尝试一次认证：

- 不创建窗口或标签页；
- 直接向统一认证站点请求登录表单、验证码挑战并提交认证；
- 浏览器会按正常网络规则维护认证 Cookie，扩展不申请 `cookies` 权限，也不读取、复制或修改 Cookie 值；
- 使用一次性超时闹钟控制最长运行时间，不做周期性保活；
- 用户主动打开认证页或退出认证时，后台流程会取消或让出控制；
- 失败后本次浏览器会话不会自动反复重试。

## 权限说明

| 权限 | 用途 |
| --- | --- |
| `storage` | 保存本地配置、凭证、监控设置，以及本次会话的预认证和课程监控恢复状态 |
| `alarms` | 为可选的后台预认证设置一次性超时，不用于周期性跟踪或保活 |
| `https://authserver.nju.edu.cn/*` | 在认证页面运行本地登录逻辑，并在用户开启预认证时直接完成认证站原有请求 |
| `https://xk.nju.edu.cn/*` | 在选课登录和课程页面运行本地登录、监控与选择逻辑 |

扩展未申请 `cookies`、`history`、`offscreen` 或可读取全部网站内容的 `<all_urls>` 主机权限。

## 数据共享

扩展不会出售、出租、共享或转让用户数据。统一身份认证和选课系统会按其自身服务逻辑接收用户主动执行登录与选课所需的数据；这不是向第三方服务额外上传。

## 用户控制

- 可以随时修改已保存的账号和密码，或关闭任一自动化功能。
- 停止课程监控不会删除已保存的关键词或精确教学班目标，但会停止后续自动检查；精确目标可在 Popup 中手动移除。
- 卸载扩展会移除当前浏览器配置文件中的扩展本地存储。卸载前如需保留非敏感配置，请自行记录。
- 浏览器账号同步、设备备份或企业管理策略可能有其独立的数据处理规则，请查阅所用浏览器的说明。

## 联系与安全报告

项目源码和普通问题反馈位于 <https://github.com/treehey/AutoCaptcha>。

发现可能泄露凭证、扩大站点权限或绕过用户控制的安全问题时，请按 [SECURITY.md](SECURITY.md) 私下报告，不要在公开 Issue 中粘贴账号、密码、Cookie、完整页面源码或未脱敏截图。
