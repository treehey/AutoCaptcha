# 更新记录

本文档记录 NJU Login Pro / NJU 自动登录助手的重要版本变化。

## Unreleased

- 暂无。

## v5.1.1 - 2026-07-26

- 修复选课点击验证码中淡色候选字在背景减除后被过严阈值裁成空白的问题。候选字定位阈值独立放宽，目标字、模型、背景模板和自动点击门槛保持不变。
- 增加候选字裁剪诊断信息，便于定位异常验证码的前景像素数量、裁剪框和回退状态。
- 使用既有 600 张点击验证码回放：整码识别由 `590/600` 提升至 `591/600`；通过自动点击门槛且正确的样本由 `561` 提升至 `565`，未新增错误自动放行。

## v5.1.0 - 2026-07-26

- 首次交付选课系统点击验证码的本地自动识别与自动点击：模型、背景模板与 ONNX/WASM 推理均随扩展离线打包，自动点击默认关闭。
- 修复选课点击验证码内容脚本无法从选课页面创建本地 Worker 的问题：通过同源 Blob 启动模块，再加载已随扩展打包的 Worker、ONNX 模型和 WASM 运行时。
- 优化点击验证码标点：编号改为显示在候选字旁，并用细边框关联目标字，避免遮住待确认的中文字符。
- “重新识别当前验证码”现在会绕过同图去重并重新运行；启用自动点击后，手动重识别也会遵循自动点击策略。
- 自动点击模式在低置信时最多换图重试 5 次；背景不匹配、模型错误、同图重复或用户关闭自动点击时不会盲目点击。
- 面板展示换图次数，并明确区分纯标点与自动点击模式的行为。

## v5.0.0 - 2026-07-11

- 登录页新增非阻塞验证码状态提示：识别中、已填入、自动登录、自动重试与失败都会给出明确反馈，不影响 OCR/CNN 推理或自动提交速度。
- 验证码识别升级为“原始彩色字符槽轻量 CNN + 低置信 Tesseract 回退”架构；CNN 随扩展打包并在浏览器本地纯 JavaScript 推理，不依赖云端服务、外部 API 或新增运行库。
- 使用 `round-024` 至 `round-029` 共 180 张样本训练，`round-030` 至 `round-033` 共 120 张未参与训练的样本作为独立留出集；插件真实流程达到 `118/120`（约 `98.3%`）。
- 发布候选冻结后新增 `round-034` 至 `round-038` 共 150 张全新验证样本，不训练、不调参并仅评测一次，结果为 `146/150`（约 `97.3%`），平均约 `0.055s/张`。
- 两批未参与训练的样本合计 `264/270`（约 `97.8%`）；v5 完整 15 轮回归为 `444/450`（约 `98.7%`），其中训练轮只用于防回归。
- `round-024` 至 `round-033` 共 300 张完整回归为 `298/300`（约 `99.3%`）；完整回归包含训练轮，泛化指标以独立留出集为准。
- 独立留出集相比 v4.2.0 默认流程从 `87/120`（约 `72.5%`）提升至 `118/120`（约 `98.3%`）。
- 热态 CNN 识别通常约 `30-45ms`；包含少量 OCR 回退的独立留出集平均约 `0.06s/张`，相比旧流程约 `0.22s/张` 明显降低。
- CNN 对首位 `c/e` 使用开发集确定的保护门控，对极低置信字符最多回退一位旧 OCR；模型加载或推理失败时自动完整回退 Tesseract。
- Popup 将“模板 OCR 增强”更新为“本地 CNN 增强”；关闭后仍可使用旧 Tesseract 流程，手动“重新识别”可继续进行 A/B 对比。
- 新增原始彩色字符 CNN 训练、导出、融合分析及 PSM/模板实验工具；验证码样本与答案表全部纳入 Git 版本管理。

## v4.2.0 - 2026-07-10

- Popup 新增“验证码测试”区域：在统一认证页可手动对当前验证码重新识别，结果只回填验证码输入框，不会自动提交登录。
- 测试结果展示各 OCR 预处理候选、最终结果、模板 OCR 增强开关状态及模板重排前后变化，便于对同一验证码进行 A/B 对比。
- 手动测试识别与页面自动识别共用互斥锁，并取消待执行的自动重试，避免并发 OCR、自动刷新或误触发提交。

## v4.1.0 - 2026-07-06

- 全面重设计 Popup 面板，改为更清晰的控制台式布局，登录助手与选课助手使用分段 Tab 切换。
- 顶部新增状态胶囊，直接展示自动登录、模板 OCR 增强和选课监控状态。
- 优化账号配置体验：新增配置状态、保存更改状态、密码显示/隐藏和即时保存反馈。
- 优化登录行为设置层级，将自动登录、识别后提交、模板增强和强制填充统一为可读性更高的设置行。
- 优化选课助手交互：新增选课页面连接状态、监控摘要、课程数量统计、刷新间隔快捷选择和未连接时一键打开选课页。
- 优化运行日志：新增日志复制、清空、颜色分级和最近日志限制，降低长时间监控时的阅读负担。
- 选课页面状态快照新增课程列表和刷新间隔字段，便于 Popup 重新打开后恢复更完整的监控状态。

## v4.0.0 - 2026-07-06

- 升级验证码识别架构：从单纯多路 Tesseract OCR + 手写形态规则，升级为“多路 OCR 候选 + 本地模板特征重排”的混合流程。
- 新增随扩展打包的本地模板特征模型 `assets/captcha-template-model.json`，模型只在浏览器本地参与 OCR 候选排序，不上传验证码图片，也不依赖大模型 API。
- Popup 新增“模板 OCR 增强”开关；关闭后可回退旧 OCR 候选选择流程，便于 A/B 对比和紧急回退。
- 新增模板模型构建脚本与回归脚本参数，支持插件真实流程、注入模型流程、leave-one-round-out 和候选重排调参。
- 模板重排默认采用 `thin/k3/margin=10`，并加入弱单路候选保护，降低模板把多路稳定 OCR 结果改错的风险。
- 优化模板推理速度：训练样本特征按模式缓存，候选距离只维护 top-k 邻居，避免每张图重复全量排序。
- 移除针对固定字符对和固定变体的窄范围 OCR 纠错规则，避免旧样本分数虚高但新样本泛化下降。
- 新增大小写不敏感的整码候选聚类与投票逻辑，更贴合验证码实际判定规则。
- 调整候选选择策略：限制 aggressive/thin 单路高置信覆盖多路稳定候选，只有在去线候选具备足够旁证时才接管。
- 调整 fallback 触发：当 fast 候选存在强近邻分歧时继续补跑 fallback，降低过早停止导致的误选。
- 新 holdout `round-016` 至 `round-019` 插件真实流程结果为 `91/120`（约 `75.8%`），平均 OCR 约 `0.20s`；同一集合下 v3.6.4 默认流程为 `78/120`（约 `65.0%`），净增 `13` 张且未引入新增改错。

## v3.6.4 - 2026-07-02

- 优化增强验证码后处理，新增 `5/S`、`6/b`、`B/8`、`3/4`、`4/d`、`T/r`、`W/n` 等高频混淆的窄范围形态纠偏。
- 新增 aggressive-line-clean 预处理变体，用于削弱横线/斜线干扰，并将其降级为带旁证的候选来源，避免单路高置信误判直接胜出。
- 重构验证码候选选择策略：对高置信单字符候选、形状纠偏和 trusted fallback 增加保护条件，减少正确候选被多数票淹没的情况。
- 增加唯一 4 位候选的漏字支持判断：当多个 3 位候选可解释为同一结果漏切一位时，不再直接丢弃低置信 4 位候选。
- 新增窄范围候选背书纠错：只在指定变体、置信度和其它字符位置一致时采用候选中的单个字符或特定双字符组合，提升困难样本命中率并避免整串误信。
- 本地 OCR 回归基准改为按实际验证码规则进行字母大小写不敏感判定。
- 新增验证码采样、标注和 benchmark 工具，便于后续用真实样本持续回归。
- 用独立 holdout 样本排查过拟合，收紧只对单轮样本有效的形态规则；当前 `round-001` 至 `round-007` 混合回归集达到 `175/210`（约 `83.3%`），平均 OCR 约 `0.24s`。

## v3.6.3 - 2026-07-01

- 新增发布打包脚本，仅打包扩展运行所需文件，避免将 `.git`、文档和其他仓库文件包含进商店包。
- 生成更小的干净发布包，降低 Edge Add-ons 分发和安装时出现解压失败的概率。

## v3.6.2 - 2026-06-19

- 更新插件商店描述文档，突出无需大模型 API、本地 OCR、隐私保护与登录后的自动抢课能力。
- 新增隐私政策文档，说明本地存储、OCR 本地处理和不上传敏感数据的边界。
- 收紧扩展权限，移除会触发“阅读浏览历史记录”提示的 `tabs` 权限，改为仅授权选课站点标签页查询。
- 更新 manifest 与本地化短描述，明确选课系统不提供点击式验证码自动登录。

## v3.6.1 - 2026-06-19

- 明确选课系统当前仅支持登录后的自动抢课，不再声明支持点击式验证码自动登录。
- 从选课系统页面注入列表移除点击式验证码自动登录脚本，仅保留自动抢课脚本。
- 移除 Popup 中灰置的选课系统自动登录开关，降低功能边界误解。
- 移除未启用的选课点击验证码脚本与中文 OCR 训练数据，减小扩展包体。

## v3.6 - 2026-06-18

- 增加 `t`/`U` 形态纠偏，针对左右竖笔与底部弧线修正 `qtfU` 被识别为 `qtft` 的情况。
- 增加无点大写 `J` 形态纠偏，针对右竖与左下钩修正 `KgJA` 被识别为 `KgiA` 的情况。
- 增加 `p`/`P` 与 `P`/`h` 形态纠偏，降低 `VaPh` 被识别为 `VapP` 的概率。
- 修正列向笔画簇统计的右边界漏计问题，提高相邻字符和底部连笔场景下的形态判断稳定性。

## v3.5 - 2026-06-18

- 调整验证码候选投票策略：`0` 置信多数票遇到更可信完整候选时不再直接胜出，降低 `MrSu` 被误选为 `MeSu` 的概率。
- 优化 strict 与 legacy 候选的同支撑决胜逻辑，降低 `99h8` 被高置信 strict 候选误选为 `STh8` 的概率。
- 增加小写 `i` 形态纠偏，针对带干扰线的窄竖笔画和顶部点修正 `H`/`E` 到 `i`，降低 `iiBQ` 被识别为 `HEBQ` 的概率。
- 收紧 `u`/`c` 大写纠偏，避免缺少大写候选证据时把 `u` 误改为 `U`。

## v3.4 - 2026-06-18

- 引入 Tesseract.js scheduler 与最多 2 个 OCR worker，并行处理多路验证码预处理结果，降低单次识别等待时间。
- 将 Tesseract 页面分割模式从单行文本调整为 raw line，更适配短验证码场景。
- 增加快路径/兜底路径：默认先跑高贡献变体，未选出结果时再补跑 `balanced-color`，在保持识别率的同时减少平均耗时。
- 优化验证码候选选择策略：优先多路一致结果，同时允许高置信完整候选放行，减少“已识别但被保守丢弃”的情况。
- 保留完整多变体识别流程，避免通过削减候选数量换速度导致成功率下降。

## v3.3 - 2026-06-18

- 优化统一身份认证图形验证码识别，针对彩色字符、横线和斜线干扰增加多路图像预处理。
- 复用并预热 Tesseract OCR worker，减少重复初始化开销。
- 增加多候选投票与高置信候选放行策略，减少错误验证码提交，同时提升平均登录速度。
- 增加 `j`/`p` 形态纠错，降低干扰线导致小写 `j` 被识别为 `p` 的概率。
- 增加 `i`/`j` 与 `B`/`D` 形态纠错，降低 `ppjD` 被误识别为 `ppiB` 的概率。
- 增加 `S`/`5` 与 `n`/`m` 形态纠错，降低 `5xam` 被误识别为 `Sxan` 的概率。
- 增加窄范围大小写形态修正，降低全高 `U`/`C` 被识别为 `u`/`c` 的概率。
- 将验证码失败后的自动刷新重试上限提高到 5 次。
- Popup 面板新增当前版本号显示，版本号从 `manifest.json` 动态读取。
- 新增本更新记录文档。

## v3.2

- 更新扩展版本号至 3.2。
- 优化统一身份认证验证码识别启动日志。
- 增强验证码识别流程，加入自动刷新机制、并发识别锁和状态管理，避免重复识别与重试堆积。

## v3.1.1

- 更新扩展版本号至 3.1.1。
- 修正存储键名，提高配置读取与保存的兼容性。
- 优化选课页面刷新列表逻辑，使用 DOM 变化监听提升稳定性。

## v3.1

- 新增选课助手，可在 Popup 中配置目标课程并持续监控余量。
- 支持课程名称模糊匹配、自动点击选择按钮、自动确认弹窗。
- 增加监控日志、刷新间隔设置和运行状态展示。
- 增强抢课逻辑，加入课程状态检查、重试机制与专业课支持。

## v3.0

- 曾尝试选课系统登录页适配，但点击式验证码自动登录未进入当前支持范围。
- 选课系统当前需手动完成登录，插件仅保留登录后的自动抢课能力。
- 保留统一身份认证系统账号密码自动填充与图形验证码识别。

## 早期版本

- 支持南京大学统一身份认证系统图形验证码 OCR。
- 支持账号密码本地保存、自动填充、强制填充和识别后自动登录。
- 提供 Chrome 扩展 Popup 配置面板。
