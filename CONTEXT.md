# NJU Login Pro 领域词汇

本项目帮助用户在南京大学认证和选课页面上执行本地自动化。本词汇表限定抢课模块中容易混淆的业务概念。

## 选课监控

**抢课任务（Grab Task）**：
用户一次启动的选课监控意图，包含一个或多个课程目标；任务配置在运行期间保持不变。
_避免：课程列表、剩余课程_

**课程目标（Course Target）**：
抢课任务中需要满足的一项选课意图，可以是关键词目标或精确教学班目标。
_避免：课程名称_

**课程组（Course Group）**：
抢课任务中的满足单位，包含一个或多个可替代或可并列满足的课程目标。
_避免：课程分类、目标列表_

**要求数量（Required Count）**：
一个课程组被视为完成前，必须二次确认已选的课程目标数量。
_避免：目标总数、课程容量_

**目标优先级（Target Priority）**：
同一抢课任务中课程目标的尝试顺序偏好；数值越高越先尝试，但不会自动退掉已经选中的低优先级目标。
_避免：保底替换、强制顺序_

**关键词目标（Keyword Target）**：
通过课程名称关键词寻找候选教学班的课程目标，适合尚未确定具体教学班的兼容入口。
_避免：模糊课程、课程字符串_

**精确教学班目标（Teaching Class Target）**：
指向一个稳定教学班标识的课程目标；课程名称、教师、时间和校区只用于展示。
_避免：精确课程、课程名称_

**候选教学班（Candidate）**：
当前选课批次中与课程目标匹配、可被观察或尝试提交的教学班。
_避免：课程行、按钮_

**已选（Selected）**：
教学班已经由服务端结果或页面的精确教学班状态二次确认选中。
_避免：已确认、已提交、点击成功_
